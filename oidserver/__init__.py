import logging


# Set the central logger. NOTE: Configuration is not performed until after the
# classes are imported, so do not include actual logging messages here.
logger = logging.getLogger('oid')
